//mimium test for array literals and array access

// Create an array with 3 elements
let arr = [10.0, 20.0, 40.0]

// Access the first element
let first = probeln(arr[0])

// Access with interpolation (should give 15.0)
let interp = arr[1]

// Modify the third element /todo: array assignment
// arr[2] = 40.0

// Return the values for testing
let out = first + interp + arr[2]
fn dsp() {
  // Returns 10.0 (first) + 20.0(second) + 40.0(third) = 70.0
  out
}
